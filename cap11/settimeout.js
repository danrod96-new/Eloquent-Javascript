
// As an example, the setTimeout function, available both in Node.js 
// and in browsers, waits a given number of milliseconds 
// (a second is a thousand milliseconds) and then calls a function.
setTimeout(() => console.log("Hola"), 1500);